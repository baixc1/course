1、vedio	//视频
		controls 	//添加控件。
		src/autoplay/width...
		source元素	//链接不同的视频
2、vedio/dom	
		play()、pause()...
3、audio
4、拖放
	<img draggable="true" />
	ondragstart,ondragover,ondrop		//事件
	dataTransfer.setData() 
5、canvas
	cxt.drawImage(imgObj,x,y,w,h)				//在画布上绘制图像、画布或视频
	cxt.fillRect()
	cxt.stroke()
	cxt.fill()
	cxt.arc(x,y,r,sAngle,eAngle)
	grad = cxt.createLinearGradient()	
	grd.addColorStop()
	cxt.fillStyle=grd;
	
6、SVG
7、地理定位
8、web存储
	localStorage   localStorage.setItem() 
	sessionStorage 	...
9、应用缓存
	cache manifest 
10、Web Worker
	typeof(Worker)
	postMessage()
11、表单输入类型