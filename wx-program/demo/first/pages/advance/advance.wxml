<!--导航条-->  
<view class="main">  
  <block wx:for="{{list}}">
    <button wx:if="{{item.type!=1}}" data-url="{{item.url}}" bindtap="changePage">{{item.text}}</button>  
    <button wx:else open-type="share">{{item.text}}</button> 
  </block>
</view>  