<view class="list" bindtap="change">
  <view wx:for="{{list}}" class="{{index == activeIndex ? 'active' : ''}}" data-index="{{index}}">{{item}}</view>
</view>
<view style="height: 1000000px">
  <image wx:if="{{index<1000}}" src="{{item}}" wx:for="{{arr}}"></image>
</view>



